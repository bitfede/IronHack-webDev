<h3>Add a new Project</h3>

<%= form_for @my_project do |f| %>
	
	<%= f.label :name %>
	<%= f.text_field :name %>
	<%= f.label :description %>
	<%= f.text_area :description %>

	<%= f.button %>
<% end %>